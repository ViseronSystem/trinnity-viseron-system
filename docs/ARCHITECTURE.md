# TVS — Architecture Map & System Graphs

> Trinnity Viseron System v5.0 — Autonomous Enterprise Operating System (AEOS)
> Orchestrated by **Pedro Costa (Commander)** and **Trinnity Hurtado (Queen)**
> Generated by the autonomous audit pipeline (Squad AIOX + ViseronCore).

## 1. Repository Map

| Area | Entries | Purpose |
|---|---|---|
| `src/` | 132 `.ts` | Core runtime (kernel, agents, memory, routing, APIs) |
| `core/` | 2 `.ts` | Autonomy & orchestration kernel |
| `agents/` | registry | Agent archetypes & battalion definitions |
| `tests/` | 3 files | Core + web + hyper-learning tests (~115 assertions) |
| `scripts/` | 48 files | Automation: PDFs, deploy, backup, audit, go-live |
| `tools/` | CUDACyclone | GPU puzzle solver (GPL) |
| `packages/` | aiox-core + | SDK packages (AIOX) |
| `mobile/` | Expo | Android APK + iOS IPA (8 screens) |
| `electron/` | main.js | Windows desktop launcher |
| `dashboard/` | src/dashboard | Web UI (port 3000) |
| `trinnityviseron.com/` + `trinnityviseronsystem.io/` | static | Public sites |
| `data/` | minds.json, workspace | 5,000 minds + tenant workspaces |
| `docs/` | 17 `.md` | Documentation + PDF manuals |

## 2. Boot Order (src/index.ts)

1. Core construction — architect/developer/security agents + squads
2. `N8NBridge.initialize()` — workflow engine (port 5678, 5 templates)
3. `spawnAllMinds()` — **5,000 minds → agents**
4. `spawnAllArchetypes()` — **246 archetypes → `arch_*` agents**
5. `SuperMind.synthesize` → `SuperIntelligence.synthesize` (4 domains)
6. `CommandChain` directives — SUPERINTELIGENCIA + EVOLUCIÓN HIPER
7. TokenEngine — $TRIN + $VSR (300M, Proof-of-Mandate)
8. `ReportServer` — port 3001 (PDF reports)
9. `SuperIntegration.initializeAll()` — 7 modules
10. Cycles — autoLearning + planner + evolution + hyperLearning
11. Servers — Dashboard (3000), ViseronWeb (32123), Terminal

## 3. Core Subsystems (src/core/)

| Subsystem | Responsibility |
|---|---|
| `TVSOrchestrator` | Process orchestration |
| `AgentManager` | Agent registry (Map + roleIndex + capabilityIndex) |
| `ModelRouter` | Provider/model selection (cost, latency, quality) |
| `MemoryEngine` | STM / LTM / Knowledge + Qdrant semantic search |
| `ToolManager` | Tool registry & execution |
| `ProviderFactory` | 6 providers: Ollama, OpenAI, Claude, Gemini, Grok, OmniRoute |
| `SquadManager` | Squad lifecycle |
| `MCPServer` | MCP tool bridge (list/execute) |
| `AgentFactory` | 8 blueprints (biz-analyst, data-scientist, fullstack, ai-engineer, devops, marketing, pm) |
| `AgentSpawner` | 5,000 minds loading |
| `SuperMind` | 10 eras (1500–3000), domain synthesis |
| `SuperIntelligenceEngine` | Ensemble strategy, +1000% baseline |
| `HyperLearningEngine` | 30-min cycle, intelligence 1000 base |
| `AutoEvolutionEngine` | 25 capabilities, 300 agents/cycle, pollination |
| `AutonomousPlanner` | CRITICAL→LOW tasks |
| `BusinessSolutionEngine` | BusinessProblem → Solution |
| `WebAppGenerator` | 9 app types × 5 frameworks |
| `CommandChain` | Authority & clearance (absolute→none) |
| `BattalionRegistry` | 114 agents (corona 59 / hierro 57) |
| `DirectiveEngine` | Dual-signature directives (Reina ratifica, Capitán comanda) |
| `VoiceBridge` | PT/EN/ES voice (Pedro/Trinnity) |
| `AICommunityPlatform` | Free/Premium/VIP/Admin monetization |

## 4. Agent Graph

```
                       CommandChain
                 Pedro Costa (Supreme Commander)
                 Trinnity Hurtado (Queen / La Corona Viva)
                            │
            ┌───────────────┴───────────────┐
       squad_executive                squad_architecture
            │                         (Trinnity + architect + developer + security)
            │
   Battalion (114 agents, depth 0..N)
        ├─ trinnity-hurtado   (La Corona Viva)
        ├─ pedro-costa        (El Yunque)
        ├─ selene-hurtado     (La Estratega)
        └─ rocio-hurtado      (La Custodia)
            └─ 114 lineage-derived squads

   5,000 Minds ──► AgentSpawner ──► 5,000 agents
   246 Archetypes ──► arch_* agents (mythological 51, biblical 57,
   classical 38, futuristic 31, antichrist 30, modern 14, medieval 13, renaissance 12)
```

## 5. Memory & Knowledge Graph

```
MemoryEngine
 ├─ STM (short-term) ──► LTM (long-term) consolidation (AutoLearning 30-min)
 ├─ LTM ──► Qdrant semantic search
 └─ Knowledge base ──► BusinessSolutionEngine + SuperMind domains
HyperLearningEngine (intelligence, x6/30min)
AutoEvolutionEngine (knowledge gain 0.01–0.05, pollination pool 120)
```

## 6. Integration Graph

| Module | Purpose | Port |
|---|---|---|
| Viseron Apps | Full engine | — |
| TVS GitHub Tools | Repo automation | — |
| OmniRoute Gateway | 290+ AI providers | 20128 |
| AI Call System | Twilio + voice | — |
| OpenJarvis | Stanford local AI | — |
| ASNO | WhatsApp + Home Assistant | — |
| n8n | Workflow engine (5 templates) | 5678 |

## 7. API Surface (Express, under /api)

- auth (6): register/login/me/profile/users/logout
- billing (4): plans/subscription/checkout/webhook (Avirato HMAC | Stripe)
- onboarding (3): templates/apply/workspace
- email (7): status/test/verify/reset/verified
- messaging (10): status/key/contacts/conversations/groups/messages/read (x25519 + aes-256-gcm)
- jarvis (2): status/chat (13 intents, 9 tools, rate-limited)
- revenue (2): readiness, ai/status
- blog (8): posts CRUD + stats
- standalone: health/metrics/waitlist/content/pitch/dashboard + Socket.IO

## 8. Dependency Graph (runtime)

```
express 5 ─ socket.io ─ pdfkit ─ pg ─ nodemailer ─ node-cron
     │         │
  zod ───── axios ── openai 7 / ollama / stripe 22 / @langchain/core
     │
  tsx / typescript 5.9 ── (build tooling)
```

## 9. Ports

| Port | Service |
|---|---|
| 3000 | TVS Dashboard |
| 3001 | ReportServer (PDF) |
| 5678 | n8n workflow engine |
| 32123 | ViseronWeb |
| 20128 | OmniRoute AI Gateway |
