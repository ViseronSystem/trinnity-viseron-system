.expo/
node_modules/
dist/
.env
*.log
generated-crypto-sites/
generated-apps/
# APKs gerados pela App Factory (ficheiros grandes; não versionar)
data/apps/*.apk
# Pacote de migração (contém .env com segredos — NUNCA versionar)
migracao/
backups/
logs/
.build/
electron/dist-electron/
electron/node_modules/
electron/staging/
data/server.log.err
mobile/android/
mobile/ios/
mobile/node_modules/
*.zip
*.7z
TVS_VISERON_MASTER_REPORT.pdf
tvs_stderr.txt
tvs_stdout.txt
tvs_demo_*.txt
tvs_demo_pid.txt
# Backup do site (deploys locais) — nunca versionar
VISERON_SITE_BACKUP/
# Logs de ciclos de autonomia (runtime)
data/reports/cycle_*.json
# Scan de segredos (artefato runtime; o veredicto entra no AUDIT_REPORT)
data/segredos-scan.json
# Estado runtime do kernel/task-queue e bases locais
data/state/task-queue.json
data/state/tvs-service.json
data/benchmarks/
database/
mobile/releases/

# Memória runtime do sistema (demasiado grande para git; backup via npm run backup)
database/memory/ltm.json

# Colecciones de skills vendered (instalables via npm run skills:install)
skills/vendor/
# Catálogo próprio de apps LLM (autoría TVS) — versionado
!skills/vendor/awesome-llm-apps/

# Backup archives are excluded; individual backup metadata stays
backups/*.zip
backups/*.7z
data/blog/posts.json

# Runtime state (gerado pelos demos/tests; backup via npm run backup)
data/accounts.json
data/messaging.json
data/jarvis-sessions.json
data/email-tokens.json
data/email-codes.json
data/workspace.json

data/Viseron_Cofre_Credenciais.pdf
data/Viseron_Auditoria_Completa.pdf

# Suite AEOS (Cofre de Acesso e artefatos gerados) — confidencial
data/AEOS_*.pdf

data/TVS_Comandos_e_Senhas.pdf

# Base 45k Telecomunicaciones — DADOS PESSOAIS (RGPD). NUNCA versionar.
# contacts.json (32.4k: DNI/nome/telefone), emails.json, sms.json (E.164)
45k telecomunicaciones.xlsx
data/telecom/contacts.json
data/telecom/emails.json
data/telecom/sms.json
tools/viseron-game/windows/__pycache__/

# Wallet Cosmos — ACESSO COMPLETO (CONFIDENCIAL)
data/Viseron_Cosmos_Wallet_ACESSO.txt

data/Viseron_Wallets_Fabrica_*.txt

# graphify — knowledge graph do VISERON (cache regeneravel)
graphify-out/cache/
