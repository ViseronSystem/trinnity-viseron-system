.expo/
node_modules/
dist/
.env
*.log
generated-crypto-sites/
generated-apps/
backups/
logs/
.build/
electron/dist-electron/
electron/node_modules/
mobile/android/
mobile/ios/
mobile/node_modules/
*.zip
*.7z
TVS_VISERON_MASTER_REPORT.pdf
tvs_stderr.txt
tvs_stdout.txt
tvs_demo_*.txt
tvs_demo_pid.txt

# Memória runtime do sistema (demasiado grande para git; backup via npm run backup)
database/memory/ltm.json

# Colecciones de skills vendered (instalables via npm run skills:install)
skills/vendor/
# Catálogo próprio de apps LLM (autoría TVS) — versionado
!skills/vendor/awesome-llm-apps/

# Backup archives are excluded; individual backup metadata stays
backups/*.zip
backups/*.7z
data/blog/posts.json

# Runtime state (gerado pelos demos/tests; backup via npm run backup)
data/accounts.json
data/messaging.json
data/jarvis-sessions.json
data/email-tokens.json
data/email-codes.json
data/workspace.json

data/Viseron_Cofre_Credenciais.pdf

data/Viseron_Auditoria_Completa.pdf
