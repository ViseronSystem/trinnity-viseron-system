{
  "audit": "VISERON P2.3 — Secret Management Audit",
  "timestamp": "2026-08-12",
  "findings": {
    "totalKeys": 36,
    "categories": {
      "payment": ["AVIRATO_API_KEY", "AVIRATO_WEBCODE", "AVIRATO_CLIENT_SECRET"],
      "communication": ["TWILIO_ACCOUNT_SID", "TWILIO_AUTH_TOKEN", "TWILIO_PHONE_NUMBER"],
      "email": ["GMAIL_CLIENT_ID", "GMAIL_CLIENT_SECRET", "GMAIL_REFRESH_TOKEN", "GMAIL_USER", "EMAIL_PROVIDER", "EMAIL_FROM"],
      "infrastructure": ["CLOUDFLARE_API_TOKEN", "CLOUDFLARE_ACCOUNT_ID", "CLOUDFLARE_ZONE_ID", "RENDER_API_KEY", "RENDER_SERVICE_ID", "RENDER_WEB_URL", "HOSTALIA_FTP_HOST", "HOSTALIA_FTP_USER", "HOSTALIA_FTP_PASS", "HOSTALIA_FTP_PATH"],
      "database": ["DATABASE_URL"],
      "security": ["TVS_JWT_SECRET"],
      "ai_integration": ["COMPOSIO_API_KEY", "OLLAMA_HOST", "OLLAMA_MODEL"],
      "configuration": ["PORT", "REPORT_PORT", "NODE_ENV", "OMNIROUTE_ENABLED", "OMNIROUTE_PORT", "OMNIROUTE_DATA_DIR", "PUBLIC_HOSTNAME", "TVS_PUBLIC_URL"]
    }
  },
  "securityAssessment": {
    "pass": [
      ".env is gitignored (checked .gitignore)",
      "No API keys found in committed TypeScript files",
      "No secrets in package.json or tsconfig.json",
      ".env.example exists without real values",
      "Crypto wallet keys in gitignored files (contracts/solana-keypair.json)"
    ],
    "warning": [
      "HOSTALIA_FTP_PASS in .env — plaintext FTP password. Consider SSH key auth instead.",
      "AVIRATO_CLIENT_SECRET — should be rotated on target server",
      "GMAIL_REFRESH_TOKEN — OAuth token, requires Gmail API re-authorization on new server"
    ],
    "recommendation": [
      "Generate new .env for production server with fresh secrets where possible",
      "Rotate AVIRATO_CLIENT_SECRET on target server",
      "Re-authorize Gmail OAuth on target server (new refresh token)",
      "Use Windows Credential Manager or environment variables for sensitive keys",
      "Never copy .env into backup zip — transfer separately via secure channel"
    ]
  },
  "migrationSecretPlan": {
    "doNotTransfer": [".env (transfer manually via secure channel)"],
    "rotateOnTarget": ["AVIRATO_CLIENT_SECRET", "GMAIL_REFRESH_TOKEN", "TVS_JWT_SECRET"],
    "keepSame": ["OLLAMA_HOST", "OLLAMA_MODEL", "PORT", "NODE_ENV"],
    "updateForTarget": ["TVS_PUBLIC_URL", "PUBLIC_HOSTNAME (if DNS changes)", "RENDER_WEB_URL"]
  }
}
